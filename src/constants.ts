// ── Grid ──────────────────────────────────────────
export const CELL_SIZE = 64;
export const GRID_LINE_WIDTH = 1;
export const GRID_LINE_COLOR = 'rgba(255, 255, 255, 0.04)';

// ── Timing ────────────────────────────────────────
export const TICK_RATE = 60;
export const TICK_MS = 1000 / TICK_RATE;

// ── Rendering ─────────────────────────────────────
export const BUTTON_PADDING = 6; // px inside cell
export const BUTTON_BORDER_RADIUS = 10;
export const HOVER_SCALE = 1.08;
export const CLICK_SCALE = 0.92;

// ── Gameplay ──────────────────────────────────────
export const MODIFIER_CHANCE = 0.25; // 25% chance on each click
export const SPAWN_ANIM_DURATION_MS = 200;
export const CLICK_ANIM_DURATION_MS = 150;

// ── Debug ─────────────────────────────────────────
export const DEBUG_MODE = true;
